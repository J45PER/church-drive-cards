{
  "name": "church-drive-cards",
  "version": "0.10.6",
  "private": true,
  "description": "Custom Lovelace cards for Church Drive Home Assistant: Battery/Gauge Zone, Alarm Panel, and Light Control.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
